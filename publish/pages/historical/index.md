# Historical rejected baseline

These are the judged 0/12 revision's stub pages, preserved verbatim. They are superseded by the current claim pages (navigation above). The judged verifier `verify_diagram_flow.py` evaluated vacuous identities (e.g. `abs(H*(1/H)-1)<1e-12`); it is retained here only for provenance.
